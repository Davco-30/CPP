#include <iostream>

using namespace std;
/// p10646 - UVa

int main()
{
    int n; //Cases
    cin >> n;

    for(int i = 0; i < n; i++){
        int length = 27;
        int m = 25:
        string pile[length];
        string hand[m];

        int y = 0;

        for(int num = 0; num < 52; num++){
            string card;
            cin >> card;

            if(num >= 27){
                hand[num] = card;
                break;
            }

            pile[num] = card;
        }

        int steps = 3;
        while(steps > 0){
            int value = pile[length][0];
            if(value >= 2 && value <= 9){

            }
        }

        cout << "\n";

        /*for(int num = 0; num < 52; num++){
            cout << pile[num] << endl;
        }*/

    }

    return 0;
}
